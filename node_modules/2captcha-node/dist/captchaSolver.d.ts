import { SolveCaptcha, ResponseCaptcha } from './types';
declare const captchaSolver: (key: string) => {
    solve: ({ image, maxAttempts, }: SolveCaptcha) => Promise<ResponseCaptcha>;
    balance: () => Promise<string>;
    report: (id: string, isValid: boolean) => Promise<boolean>;
};
export default captchaSolver;
